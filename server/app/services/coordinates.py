"""Coordinate transforms.

TODO(phase-1):
    - ICRS (Gaia catalog frame) -> AltAz (observer frame) using
      `astropy.coordinates.SkyCoord` + `EarthLocation` + `Time`.
    - Apply proper motion correction from Gaia epoch J2016.0 to observation epoch.
"""
